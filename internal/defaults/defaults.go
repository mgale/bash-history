package defaults

const (
	CollectionName    string = "bash-history"
	BashHistoryTopic  string = "bash-history"
	DocumentSyncTopic string = "document-sync"
	CommandTopic      string = "command"
	CommandMaxLen     int    = 400
)
